![image](https://github.com/user-attachments/assets/d7674740-7613-4b67-a928-c1a060611338)# Functions in Python: Modulo Calculator

## 🎯 Aim
To write a Python program that defines a function which accepts two values and returns their **modulo** using the `%` operator.

## 🧠 Algorithm
1. Define a function called `result` that takes two arguments `a` and `b`.
2. Inside the function, compute the modulo using `a % b`.
3. Print the result of the modulo operation.
4. Get two integer inputs from the user.
5. Call the `result` function with the user-provided values.

## 🧾 Program
```
# Define the function
def result(a, b):
    mod = a % b
    print("Result of modulo operation:", mod)

# Get two integer inputs from the user
num1 = int(input("Enter the first number (a): "))
num2 = int(input("Enter the second number (b): "))

# Call the function with user-provided values
result(num1, num2)

```

## Output
![image](https://github.com/user-attachments/assets/ad052945-4cc6-45a8-a88e-8dc8a1d752aa)

## Result
Program executed successfully
